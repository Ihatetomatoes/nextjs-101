import React from "react";

const Module = ({ module }) => (
  <li className="mt-4 relative z-10 border border-gray-200">
    <h3 className="p-2 px-4 block bg-primaryGray font-bold">{module}</h3>
  </li>
);

export default Module;
